import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    Imagem: {
        width: 90,
        height: 90,
        left: 15,
        borderRadius: 55,
        marginTop: 15
    }

  })

  
  export default styles;